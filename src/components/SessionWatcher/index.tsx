/**
 * Created by: Oleksandr Bezrukov
 * Creation date: 9 April 2020
 *
 * The entry point of SessionWatcher component
 * which after first render send request
 * which check is user already logged in.
 */

/** Application's imports */
import Component from './Component';
import container from './container';

/** Export configured component */
export default container(Component);
